# 项目经历 — Unity UI 框架与资源管理系统

> 使用说明：本文件是可直接粘贴进简历的「项目经历」段落。`【】` 内为占位符，请替换为真实信息。
> 文中所有技术描述均与代码实现一一对应，面试可逐条展开，无虚构内容。

---

## 项目概况

| 项 | 内容 |
| --- | --- |
| 项目名称 | 【例如：Unity 动作游戏 Demo / 自研客户端基础框架】 |
| 时间 | 【例如：2024.XX – 至今】 |
| 担任角色 | 客户端开发（Unity / C#） |
| 技术栈 | Unity (URP)、C#、VContainer（依赖注入）、UniTask、Addressables、UGUI |

---

## 项目描述

独立设计并实现 Unity 客户端基础框架中的两大核心模块——**UI 框架**与**资源管理系统**。

- UI 框架采用「纯 C# 视图类 + 依赖注入」架构，将界面逻辑与 Unity 生命周期解耦，实现统一视图生命周期、多层级界面管理、弹窗栈、视图/组件缓存与事件驱动的界面刷新；
- 资源管理系统封装 Addressables 构建引用计数式缓存，并实现通用对象池与 Prefab 池体系，结合启动预热机制，降低运行时加载与实例化开销，减少高频对象创建带来的卡顿与 GC 压力。

---

## 核心职责与产出

### 一、UI 框架

1. **设计统一视图生命周期**：抽象 `IView` 接口与 `BaseView` / `BaseWidget` 基类，定义 `Initialize → Show → Hide → Destroy` 标准流程，并提供 `OnInit / OnShown / OnHidden / OnDestroy` 扩展钩子，使页面与可复用控件共享同一套开关与销毁逻辑。

2. **实现四级 UI 分层与弹窗栈**：基于 `Bottom / Middle / Top / Pop` 四级 Canvas 层级组织界面，弹窗（Pop 层）入栈管理，支持按栈序关闭与一键全关；并通过嵌套 Canvas + `overrideSorting` 实现视图内独立渲染顺序。

3. **视图与组件双重缓存**：以 prefab 路径为 key 缓存视图实例，避免重复加载与重复实例化；以「路径 + 组件类型」为 key 缓存 `GetComponent` 结果，减少运行时查找开销。

4. **视图逻辑与展示解耦**：视图采用纯 C# 类（非 MonoBehaviour），通过 `Transform` 引用操作 UI；配合 VContainer 反射扫描自动注册视图（Transient）与数据模型（Singleton），实现依赖可注入、逻辑可测试。

5. **事件驱动的界面刷新**：基于结构体泛型事件中心（`EventCenter`）实现「数据变更 → 派发消息 → 视图刷新」的响应式更新，降低界面与业务逻辑的耦合度。

### 二、资源管理

1. **封装 Addressables，实现引用计数式资源缓存**：`Load / Release` 成对管理，引用计数归零才真正释放，避免重复加载与提前释放导致的引用失效；并提供无缓存加载与批量并行加载（`UniTask.WhenAll`）路径。

2. **句柄有效性校验与自动恢复**：缓存命中时校验句柄 `IsValid + Status`，对失效句柄自动释放并重新加载，规避持有已销毁资源导致的异常。

3. **通用对象池 + Prefab 池体系**：实现通用对象池核心 `ObjectPool<T>`（预热、容量上限、双重 Get/Release 检测，可即时捕获「重复借出 / 重复归还」）；封装 `PrefabPool<T>` 泛型 Prefab 池与 `PrefabPoolManager` 多池注册表，实现「异步加载一次、同步池化多次」。

4. **启动预热机制**：通过 `PrewarmRegister` 预热表集中配置敌人、特效等高频对象，游戏启动时预加载，降低首次生成时的卡顿。

---

## 专业技能（与项目相关，可并入简历技能栏）

- 熟悉 Unity UGUI 与 UI 框架设计：生命周期管理、分层/弹窗栈、视图缓存、事件驱动刷新。
- 熟悉依赖注入框架（VContainer）与基于反射的自动注册。
- 熟悉 Addressables 资源管理与引用计数缓存、异步加载（UniTask）。
- 熟悉对象池（Object Pooling）设计与常见坑（重复借出/归还、预热、容量控制）。
- 熟悉 C# 泛型、反射、委托与事件机制。
